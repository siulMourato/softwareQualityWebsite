pipeline{
    agent any
    stages {
        stage('staging') {
            steps {
                echo "staging"
      // One or more steps need to be included within the steps block.
      }
     }

  stage('production')
  {
    steps {
         echo "production"
      // One or more steps need to be included within the steps block.
    }
  }
  }
}
